"""Constants for modelops-bundle."""

# Project marker directory
MODELOPS_BUNDLE_DIR = ".modelops-bundle"

# Configuration files (inside MODELOPS_BUNDLE_DIR)
CONFIG_FILE = "config.yaml"
TRACKED_FILE = "tracked"
STATE_FILE = "state.json"

# Default values
DEFAULT_TAG = "latest"
DEFAULT_ARTIFACT_TYPE = "application/vnd.modelops.bundle.v1"

# Version
BUNDLE_VERSION = "0.1.0"